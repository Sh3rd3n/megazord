---
phase: 01-plugin-scaffold-and-build-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsdown.config.ts
  - biome.json
  - .claude-plugin/plugin.json
  - hooks/hooks.json
  - skills/help/SKILL.md
  - skills/init/SKILL.md
  - skills/plan/SKILL.md
  - skills/go/SKILL.md
  - skills/status/SKILL.md
  - skills/pause/SKILL.md
  - skills/resume/SKILL.md
  - skills/quick/SKILL.md
  - skills/review/SKILL.md
  - skills/debug/SKILL.md
  - skills/verify/SKILL.md
  - skills/discuss/SKILL.md
  - skills/map/SKILL.md
autonomous: true
requirements:
  - DIST-01
  - DIST-04
  - PROJ-12

must_haves:
  truths:
    - "Plugin manifest exists with name 'mz' producing /mz: prefixed skills"
    - "/mz:help SKILL.md is model-invocable and lists all 13 skills with status"
    - "All 12 stub skills have disable-model-invocation: true to prevent premature loading"
    - "Plugin structure matches Claude Code conventions (skills/, agents/, hooks/, .claude-plugin/)"
    - "No SessionStart hook injects content — zero framework overhead beyond native skill descriptions"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with mz namespace"
      contains: '"name": "mz"'
    - path: "package.json"
      provides: "npm package definition with bin entry and files list"
      contains: '"megazord"'
    - path: "skills/help/SKILL.md"
      provides: "Functional help skill listing all 13 skills"
      min_lines: 20
    - path: "tsconfig.json"
      provides: "TypeScript configuration for CLI compilation"
      contains: '"strict": true'
    - path: "tsdown.config.ts"
      provides: "Build configuration for CLI bundling"
      contains: "src/cli/index.ts"
    - path: "hooks/hooks.json"
      provides: "Empty hooks — no SessionStart injection"
  key_links:
    - from: "package.json"
      to: "bin/megazord.mjs"
      via: "bin field"
      pattern: '"bin".*megazord'
    - from: ".claude-plugin/plugin.json"
      to: "skills/**/SKILL.md"
      via: "Claude Code auto-discovery"
      pattern: '"name": "mz"'
    - from: "tsdown.config.ts"
      to: "src/cli/index.ts"
      via: "entry field"
      pattern: "entry.*src/cli/index.ts"
---

<objective>
Create the Megazord project scaffold with plugin manifest, build configuration, and all 13 skill SKILL.md files (1 functional + 12 stubs).

Purpose: Establish the Claude Code plugin structure so that Megazord is recognizable by Claude Code's plugin system, all skills are discoverable via `/mz:help`, and the project is ready for TypeScript CLI development in Plan 02.

Output: Complete plugin directory structure with package.json, TypeScript/build configs, plugin.json manifest, hooks.json, and 13 skill files.
</objective>

<execution_context>
@/Users/sh3rd3n/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sh3rd3n/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffold-and-build-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project scaffold with package.json and build configs</name>
  <files>
    package.json
    tsconfig.json
    tsdown.config.ts
    biome.json
    .claude-plugin/plugin.json
    hooks/hooks.json
  </files>
  <action>
Create the project foundation files:

**package.json** — npm package definition:
- `"name": "megazord"`, `"version": "0.1.0"`, `"type": "module"`
- `"description": "Claude Code framework unifying project management, code quality, and multi-agent coordination"`
- `"engines": { "node": ">=22" }`
- `"bin": { "megazord": "bin/megazord.mjs" }`
- `"files": ["bin", "dist", "skills", "agents", "hooks", ".claude-plugin"]`
- Scripts: `"build": "tsdown"`, `"typecheck": "tsc --noEmit"`, `"test": "vitest"`, `"lint": "biome check ."`, `"format": "biome format --write ."`
- Dependencies: `commander@^14`, `zod@^4`, `picocolors@^1`, `ora@^8`, `fs-extra@^11`, `gray-matter@^4`
- DevDependencies: `typescript@~5.8`, `tsdown@^0.20`, `vitest@^4`, `@biomejs/biome@^2.3`, `@types/fs-extra@^11`, `@types/node@^22`
- `"license": "MIT"`

**.claude-plugin/plugin.json** — Claude Code plugin manifest:
- `"name": "mz"` (NOT "megazord" — this controls the `/mz:` prefix)
- `"version": "0.1.0"`
- `"description": "Unified framework for project management, code quality, and multi-agent coordination"`
- Standard fields: author, homepage, repository, license, keywords

**tsconfig.json** — TypeScript configuration:
- `"target": "ES2022"`, `"module": "NodeNext"`, `"moduleResolution": "NodeNext"`
- `"strict": true`, `"outDir": "dist"`, `"rootDir": "src"`
- `"include": ["src"]`, `"exclude": ["node_modules", "dist", "bin"]`
- `"resolveJsonModule": true`, `"esModuleInterop": true`

**tsdown.config.ts** — Build configuration:
- Entry: `["src/cli/index.ts"]`
- outDir: `"bin"`, format: `"esm"`, clean: true, dts: false
- Banner: `{ js: "#!/usr/bin/env node" }`
- External: `["commander", "zod", "picocolors", "ora", "fs-extra", "gray-matter"]`

**biome.json** — Linter/formatter config:
- Formatter: indent style tabs, line width 100
- Linter recommended rules enabled
- Organize imports enabled

**hooks/hooks.json** — Empty hooks object:
- `{ "hooks": [] }` — Intentionally empty. No SessionStart hook. This is critical for PROJ-12 (context budget). Megazord MUST NOT inject content at session start.
  </action>
  <verify>
Verify all 6 files exist and contain expected content:
- `cat package.json | grep '"name": "megazord"'` succeeds
- `cat .claude-plugin/plugin.json | grep '"name": "mz"'` succeeds
- `cat tsconfig.json | grep '"strict": true'` succeeds
- `cat tsdown.config.ts | grep 'src/cli/index.ts'` succeeds
- `cat hooks/hooks.json | grep '"hooks"'` succeeds
- `cat biome.json` shows valid JSON
  </verify>
  <done>
All 6 foundation files exist with correct content. package.json has bin pointing to bin/megazord.mjs. Plugin manifest has name "mz". Hooks are empty (no SessionStart injection). Build config targets src/cli/index.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create all 13 skill SKILL.md files</name>
  <files>
    skills/help/SKILL.md
    skills/init/SKILL.md
    skills/plan/SKILL.md
    skills/go/SKILL.md
    skills/status/SKILL.md
    skills/pause/SKILL.md
    skills/resume/SKILL.md
    skills/quick/SKILL.md
    skills/review/SKILL.md
    skills/debug/SKILL.md
    skills/verify/SKILL.md
    skills/discuss/SKILL.md
    skills/map/SKILL.md
  </files>
  <action>
Create 13 skill directories under `skills/`, each with a `SKILL.md` file.

**skills/help/SKILL.md** — The ONLY functional skill in Phase 1:
- Frontmatter: `name: help`, `description: Show all available Megazord skills with descriptions and usage examples`, `disable-model-invocation: false` (this skill SHOULD be model-invocable so Claude can suggest it)
- Body: Full skill reference table listing all 13 skills with name, description, and status (Available/Coming soon)
- Include "About Megazord" section with version 0.1.0
- Use the exact content from the research code example for `/mz:help`

**12 stub skills** — Each follows this template:
- Frontmatter: `name: {skill-name}`, `description: {one-line description}`, `disable-model-invocation: true` (CRITICAL: prevents Claude from loading stubs)
- Body: "This skill is not yet available. It will be implemented in Phase {N}."
- Brief description of what it will do
- "Run `/mz:help` to see all available skills and their status."

Skill list with descriptions and target phases:

| Skill | Description | Phase |
|-------|-------------|-------|
| init | Initialize a Megazord project with configuration and planning structure | 2 |
| plan | Plan a phase into executable tasks with dependencies and waves | 3 |
| go | Execute the current phase plan with subagent delegation | 4 |
| status | Show project progress, current phase, and next actions | 3 |
| pause | Save session context for handoff to a future session | 3 |
| resume | Restore context from a previous session and continue work | 3 |
| quick | Run a quick task without project ceremony | 3 |
| review | Two-stage code review (spec compliance + code quality) | 5 |
| debug | Systematic four-phase debugging (reproduce, isolate, root-cause, fix) | 7 |
| verify | Verify phase deliverables match acceptance criteria | 5 |
| discuss | Socratic brainstorming to explore approaches before implementation | 7 |
| map | Analyze existing codebase for brownfield project support | 8 |

Per user decision: all skills use `/mz:` prefix (flat namespace, no aliases, no shortcuts).
  </action>
  <verify>
Verify all 13 skills exist and have correct frontmatter:
- `find skills -name "SKILL.md" | wc -l` returns 13
- `grep -l "disable-model-invocation: false" skills/*/SKILL.md` returns only `skills/help/SKILL.md`
- `grep -l "disable-model-invocation: true" skills/*/SKILL.md | wc -l` returns 12
- Each stub contains "not yet available" text
- `/mz:help` SKILL.md contains a table with all 13 skills
  </verify>
  <done>
13 skill directories exist under skills/. help/SKILL.md is model-invocable and lists all skills. 12 stub skills have disable-model-invocation: true. All descriptions are concise. No SessionStart hook loads any skill content.
  </done>
</task>

</tasks>

<verification>
1. Plugin structure matches Claude Code conventions:
   - `.claude-plugin/plugin.json` exists with `"name": "mz"`
   - `skills/` directory contains 13 subdirectories, each with SKILL.md
   - `hooks/hooks.json` exists and is empty (no SessionStart)
   - No `agents/` directory yet (deferred to later phases per research)

2. Namespace isolation is ensured:
   - Plugin name "mz" produces `/mz:` prefix — no collision with `/gsd:` or `/superpowers:`
   - All skills are self-contained with no cross-plugin references

3. Context budget design is correct:
   - 12 stubs use `disable-model-invocation: true`
   - Only `/mz:help` is model-invocable
   - hooks.json has no SessionStart content injection
   - ~13 skill descriptions at ~150 chars each = ~1,950 chars << 15% of context
</verification>

<success_criteria>
- All 6 config/manifest files exist and are valid
- All 13 skills exist with correct frontmatter
- Plugin name is "mz" (not "megazord")
- Zero SessionStart hooks
- Project is ready for TypeScript CLI development (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffold-and-build-pipeline/01-01-SUMMARY.md`
</output>
